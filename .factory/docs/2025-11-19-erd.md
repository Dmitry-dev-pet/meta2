Проблема: фон SVG остаётся светлым, даже при theme: dark. Решение: передавать фоновый цвет рендереру и усилить тёмные цвета таблиц.
Что сделаю:
- Добавлю флаг `--background <css-color>` в `.factory/commands/mermaid-validate.sh` (пробрасываю в mmdc как `-b`), и в `.factory/commands/mermaid-create.sh` (прокину во время валидации/рендера). По умолчанию: если theme=dark, использовать `#0F172A`.
- Обновлю `erd-3levels-symmetric.md` themeVariables (fill/border/text/lines) для повышенного контраста.
- Перерендерю SVG и проверю, что фон и таблицы действительно тёмные.
Подтвердите — применяю эти правки?