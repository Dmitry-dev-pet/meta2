Что сделать (без легенды):
- Добавить флаг `--orientation horizontal|vertical` как удобную обёртку над `--direction`.
- Маппинг: `horizontal → LR`, `vertical → TB`.
- Приоритет: если указаны оба, приоритет у `--direction` (ручная настройка важнее).
- Значение по умолчанию не меняем (как сейчас через `--direction`, default LR).
- Внедрю флаг в mermaid-create.sh, без правок других файлов; проверю двумя примерами рендера (LR и TB).