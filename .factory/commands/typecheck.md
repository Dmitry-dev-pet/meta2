---
description: Run mypy type checking (fix nothing; check-only) with a concise summary
argument-hint: [paths] [--strict] [extra mypy args]
---

Use the executable command to type-check:

- `/typecheck.sh [paths] [--strict] [extra mypy args]`

Defaults target `data-importer/src` if no paths are provided.
